	<div class="container-fluid">
		<div class="row-fluid">
			<div class="span2">
	 
	 <ul class="btn-group btn-group-vertical">>
	 <% for @random in @sym2 
		 if @random == @answer
			x = cards_right_path %>
			<li class="btn btn-small btn-info"><%= link_to "#{@random}", x %></li>
			<% else 
				x = cards_wrong_path %>
			
				<li class="btn btn-small btn-info"><%= link_to "#{@random}", x %></li>
	 <% end %>
	  <% end %>
	</ul>
			</div>
	
	<div class= "span10"><p><%= "#{@sym}" %></p>
	</div>
		</div>
	</div>

	<div class= "span6 offset4">
	<h2><%= @time %></h2>
	</div>